# Required only for successful join-application delivery.
# Create a webhook for a private, access-controlled Discord channel.
DISCORD_JOIN_WEBHOOK_URL=

# Optional Playwright target. The default is http://localhost:3000.
PLAYWRIGHT_BASE_URL=
